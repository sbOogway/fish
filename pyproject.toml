[project]
name = "fishing-encyclopedia"
version = "0.1.0"
description = "Public fishing encyclopedia — FishBase, FishFYI, and FishRadar data"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "markdown-it-py>=4.0",
    "jinja2>=3.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.0",
]
